import { ProductPlaceholder } from "@/components/app/product-placeholder"

export default function LeagueConnectionsPage() {
  return (
    <ProductPlaceholder
      title="League connections"
      description="Connect leagues via platform adapters. Sleeper is the first practical integration; ESPN, Yahoo, and DraftKings can appear as roadmap options until reliable import paths exist."
    />
  )
}
